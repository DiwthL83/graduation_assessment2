<h3>
	<%= @contact.cont_fname %> <%= @contact.cont_lname %> | Last Contact:
</h3>

<p>
	First: <%= @contact.cont_fname %><br>
	Last: <%= @contact.cont_lname %>
</p>

<p>
	Phone: <%= @contact.cont_phone %><br>
	Email: <%= @contact.cont_email %><br>
	Tag: <%= @contact.cont_tag %>
</p>

<h4 class="">Contact History</h4>

<table>
	<% @log_entries.each do |x| %>
		<tr>
			<td><%= x.created_at %></td>
			<td><%= x.notes %></td>
			<td><%= link_to 'Edit', edit_contact_log_path(x) %></td>
			<td><%= link_to 'Delete', delete_contact_log_path(x) %></td>
		</tr>
	<% end %>
</table>

<%= render 'contact_logs/form' %>